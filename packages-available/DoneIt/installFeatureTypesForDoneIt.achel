# Install the feature types to be used with semantics. ~ semantics,internal,install
#onDefine registerForEvent Install,featureTypes,installFeatureTypesForDoneIt

createFeatureType generateTasks,,task,Generates a list of tasks.
createFeatureType generateDids,,did,Generates a list of did entries.
createFeatureType generateAchievements,,achievement,Generates a list of achievement entries.

