# Amend the state of every item in the resultSet. Eg --plate --refine=doneIt --plateState=active ~ plate
#onDefine aliasFeature plateState,pState

parameters state

loop
    set Result,state,~!Local,state!~
