# Search tasks. Much link list, but will search every field, not just shortDesc. --searchTasks=search ~ doneit
#onLoaded setFeatureType searchTasks,generateTasks

parameters search

load doneItLoad

retrieveResults DoneIt,tasks
refine ~!Local,search!~
